# Руководство по созданию базы данных в MySQL Workbench

## Шаг 1: Открытие MySQL Workbench

1. Запустите **MySQL Workbench**
2. Вы увидите список подключений (если они уже созданы)
3. Если подключения нет, создайте его (см. ниже)

## Шаг 2: Создание подключения (если его нет)

1. Нажмите на **"+"** рядом с "MySQL Connections" или кнопку **"Create a new connection"**
2. Заполните параметры:
   - **Connection Name**: `Local MySQL` (или любое имя)
   - **Hostname**: `localhost` (или `127.0.0.1`)
   - **Port**: `3306` (стандартный порт MySQL)
   - **Username**: `root` (или ваш пользователь MySQL)
   - **Password**: Нажмите **"Store in Keychain"** и введите ваш пароль MySQL
3. Нажмите **"Test Connection"** чтобы проверить подключение
4. Если тест успешен, нажмите **"OK"**

## Шаг 3: Подключение к серверу

1. Дважды кликните на созданное подключение
2. Введите пароль, если потребуется
3. Вы подключитесь к MySQL серверу

## Шаг 4: Создание базы данных

### Вариант 1: Через SQL запрос

1. В верхней части окна найдите вкладку с SQL редактором
2. Введите следующий SQL код:

```sql
-- Создание базы данных
CREATE DATABASE IF NOT EXISTS tree_encyclopedia 
CHARACTER SET utf8mb4 
COLLATE utf8mb4_unicode_ci;

-- Использование базы данных
USE tree_encyclopedia;

-- Создание пользователя (опционально)
CREATE USER IF NOT EXISTS 'tree_user'@'localhost' IDENTIFIED BY 'your_password_here';

-- Предоставление прав пользователю
GRANT ALL PRIVILEGES ON tree_encyclopedia.* TO 'tree_user'@'localhost';
FLUSH PRIVILEGES;
```

3. Выделите весь код (Ctrl+A)
4. Нажмите кнопку **⚡ Execute** (или Ctrl+Enter)
5. Проверьте результат в нижней панели - должно быть "Success"

### Вариант 2: Через интерфейс

1. В левой панели найдите раздел **"Schemas"**
2. Правой кнопкой мыши нажмите на пустое место в разделе Schemas
3. Выберите **"Create Schema..."**
4. Введите имя: `tree_encyclopedia`
5. В выпадающем списке **"Charset"** выберите: `utf8mb4`
6. В выпадающем списке **"Collation"** выберите: `utf8mb4_unicode_ci`
7. Нажмите **"Apply"**
8. В окне подтверждения нажмите **"Apply"** еще раз

## Шаг 5: Проверка создания базы данных

1. В левой панели в разделе **"Schemas"** вы должны увидеть базу данных `tree_encyclopedia`
2. Раскройте её, чтобы увидеть структуру (пока таблиц не будет)

## Шаг 6: Настройка .env файла

1. Откройте файл `.env` в корне проекта (или создайте его из `.env.example`)
2. Установите следующие значения:

```
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=ваш_пароль_mysql
DB_NAME=tree_encyclopedia
```

**Или если вы создали отдельного пользователя:**

```
DB_HOST=localhost
DB_USER=tree_user
DB_PASSWORD=your_password_here
DB_NAME=tree_encyclopedia
```

## Шаг 7: Создание таблицы (автоматически)

Таблица `users` будет создана автоматически при первом запуске сервера. Но вы можете создать её вручную:

```sql
USE tree_encyclopedia;

CREATE TABLE IF NOT EXISTS users (
    id INT AUTO_INCREMENT PRIMARY KEY,
    username VARCHAR(255) UNIQUE NOT NULL,
    email VARCHAR(255) UNIQUE NOT NULL,
    password VARCHAR(255) NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_username (username),
    INDEX idx_email (email)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

## Проверка подключения

1. Убедитесь, что MySQL сервер запущен
2. Запустите приложение: `npm start`
3. В консоли должно появиться:
   ```
   ✅ Connected to MySQL database
   ✅ Database tables initialized
   🚀 Server is running on http://localhost:3000
   📊 Using MySQL 8.0.44 database
   ```

## Решение проблем

### Ошибка: "Access denied"
- Проверьте правильность пароля в `.env`
- Убедитесь, что пользователь имеет права на базу данных
- Попробуйте использовать `root` пользователя для тестирования

### Ошибка: "Can't connect to MySQL server"
- Убедитесь, что MySQL сервер запущен
- Проверьте, что порт 3306 не заблокирован файрволом
- Проверьте, что `DB_HOST=localhost` в `.env`

### Ошибка: "Unknown database"
- Убедитесь, что база данных `tree_encyclopedia` создана
- Проверьте правильность имени в `.env` (DB_NAME)

## Полезные SQL команды для проверки

```sql
-- Показать все базы данных
SHOW DATABASES;

-- Использовать базу данных
USE tree_encyclopedia;

-- Показать все таблицы
SHOW TABLES;

-- Показать структуру таблицы users
DESCRIBE users;

-- Показать все пользователи
SELECT * FROM users;
```
